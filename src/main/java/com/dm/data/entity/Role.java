package com.dm.data.entity;

/**
 * Defines the roles a user can have within the system.
 */
public enum Role {
    /**
     * A teacher who gives courses.
     */
    TEACHER,
    /**
     * A secretary who manages administrative tasks.
     */
    SECRETARY,
    /**
     * An administrator with full system access.
     */
    ADMIN
}
